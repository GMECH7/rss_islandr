# Risk Screening System

##### How to download

git clone https://github.com/GMECH7/rss_islandr.git

##### Create a local virtual environment using the requirements.txt

cd <local project directory>
python -m venv island_env

The virtual environment should be activated due to the .vscode/settings.json

pip install -r requirements.txt

#### Note this should be changed and poetry being used instead thus discarding requirements.txt
